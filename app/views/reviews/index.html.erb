<% content_for :title, "Reviews for #{@book.title}" %>

<div class="mb-4">
  <%= link_to @book, class: "inline-flex items-center text-sm text-slate-600 hover:text-slate-900 transition-colors" do %>
    <span class="mr-1">&larr;</span> Back to <%= @book.title %>
  <% end %>
</div>

<h1 class="page-heading">Reviews for <%= @book.title %></h1>

<%= render "reviews/reviews_list", book: @book %>
