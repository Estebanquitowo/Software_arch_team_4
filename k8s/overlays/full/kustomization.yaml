apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: software-arch-team4

# Variant 4: app + Redis cache + Meilisearch
#   bin/k8s-up full
resources:
  - ../../base
  - search-config.yaml
  - redis-deployment.yaml
  - redis-service.yaml
  - redis-pvc.yaml
  - meilisearch-deployment.yaml
  - meilisearch-service.yaml
  - meilisearch-pvc.yaml

patches:
  - path: web-patch.yaml
