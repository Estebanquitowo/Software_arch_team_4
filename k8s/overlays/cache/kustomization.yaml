apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: software-arch-team4

# Variant 2: app + Redis cache (SEARCH_ENABLED stays false from base ConfigMap)
#   bin/k8s-up cache
resources:
  - ../../base
  - redis-deployment.yaml
  - redis-service.yaml
  - redis-pvc.yaml

patches:
  - path: web-patch.yaml
