apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: software-arch-team4
spec:
  template:
    spec:
      containers:
        - name: web
          env:
            - name: CACHE_ENABLED
              value: "true"
            - name: REDIS_URL
              value: "redis://redis:6379/0"
