apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: software-arch-team4

# Variant 3: app + Meilisearch (CACHE_ENABLED stays false from base)
#   bin/k8s-up search
resources:
  - ../../base
  - search-config.yaml
  - meilisearch-deployment.yaml
  - meilisearch-service.yaml
  - meilisearch-pvc.yaml

patches:
  - path: web-patch.yaml
